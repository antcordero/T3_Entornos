/*
package test;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

class OperacionesTest {

    private Operaciones operaciones;

    @BeforeEach
    void setUp() {
        operaciones = new Operaciones();
    }

    @Test
    void testSumar() {
        assertEquals(5, operaciones.sumar(2, 3), "La suma de 2 + 3 debería ser 5");
    }

    @Test
    void testRestar() {
        assertEquals(2, operaciones.restar(5, 3), "La resta de 5 - 3 debería ser 2");
    }

    @Test
    void testMultiplicar() {
        assertEquals(10, operaciones.multiplicar(2, 5), "La multiplicación de 2 * 5 debería ser 10");
    }

    @Test
    void testDividir() {
        assertEquals(2, operaciones.dividir(10, 5), "La división de 10 / 5 debería ser 2");
    }

    @Test
    void testDividirPorCero() {
        assertThrows(ArithmeticException.class, () -> operaciones.dividir(10, 0), "Dividir por cero debería lanzar una excepción");
    }
}
*/
